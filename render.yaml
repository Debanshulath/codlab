services:
  - type: web
    name: codlab
    env: node
    plan: free
    rootDir: codlab-live
    buildCommand: npm ci && npm run build
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
